<UserControl x:Class="OpenCodeLab.Views.SchedulerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource CardBrush}" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Lab:" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding LabName, UpdateSourceTrigger=PropertyChanged}" Width="150" Padding="5" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Grid.Column="2" Content="Refresh" Command="{Binding RefreshCommand}" Width="80" Padding="5" Style="{StaticResource UniformButton}"/>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- Tasks List -->
            <Border Grid.Column="0" Background="{StaticResource CardBrush}" Padding="10" Margin="0,0,10,0" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Scheduled Tasks" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>
                    <ListView Grid.Row="1" ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask}" BorderThickness="0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Name}" FontWeight="Medium" Margin="0,0,10,0"/>
                                                <TextBlock Text="{Binding TaskType}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                <TextBlock Text="Cron: " FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"/>
                                                <TextBlock Text="{Binding CronExpression}" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,10,0"/>
                                                <TextBlock Text="Next: " FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"/>
                                                <TextBlock Text="{Binding NextRun, StringFormat='{}{0:MM/dd HH:mm}'}" FontSize="10" Foreground="{StaticResource PrimaryBrush}"/>
                                            </StackPanel>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <TextBlock Text="{Binding IsEnabled}" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <TextBlock Text="&#x2713;" FontSize="14" Foreground="Green" Visibility="{Binding IsEnabled, Converter={StaticResource BoolToVis}}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>

            <!-- Right Panel - New Task & History -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- New Task Form -->
                <Border Grid.Row="0" Background="{StaticResource CardBrush}" Padding="10" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="New Task" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>
                        <TextBlock Text="Name:" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NewTaskName, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,0,8"/>
                        <TextBlock Text="Type:" Margin="0,0,0,3"/>
                        <ComboBox ItemsSource="{Binding TaskTypes}" SelectedItem="{Binding NewTaskType}" Padding="5" Margin="0,0,0,8"/>
                        <TextBlock Text="Schedule (cron):" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NewCronExpression, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,0,8"/>
                        <CheckBox Content="Enabled" IsChecked="{Binding NewTaskEnabled}" Margin="0,0,0,10"/>
                        <Button Content="Add Task" Command="{Binding AddTaskCommand}" Padding="10,5" Style="{StaticResource UniformButton}"/>
                    </StackPanel>
                </Border>

                <!-- History -->
                <Border Grid.Row="1" Background="{StaticResource CardBrush}" Padding="10" Margin="0,10,0,0" CornerRadius="5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Recent History" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>
                        <ListView Grid.Row="1" ItemsSource="{Binding History}" BorderThickness="0">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="5">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#x2713;" FontSize="12" Foreground="Green" Margin="0,0,5,0" Visibility="{Binding Success, Converter={StaticResource BoolToVis}}"/>
                                                <TextBlock Text="&#x2717;" FontSize="12" Foreground="Red" Margin="0,0,5,0" Visibility="{Binding Success, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}"/>
                                                <TextBlock Text="{Binding ExecutedAt, StringFormat='{}{0:MM/dd HH:mm}'}" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Message}" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="1" Background="{StaticResource CardBrush}" Padding="10" VerticalAlignment="Bottom" Margin="10" CornerRadius="5">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding StatusMessage}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                <Button Content="Run Selected" Command="{Binding RunNowCommand}" Padding="8,3" Margin="20,0,0,0" Style="{StaticResource UniformButton}"/>
                <Button Content="Delete Selected" Command="{Binding DeleteTaskCommand}" Padding="8,3" Margin="5,0,0,0" Style="{StaticResource RedButton}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
