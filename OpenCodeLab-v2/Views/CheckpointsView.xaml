<UserControl x:Class="OpenCodeLab.Views.CheckpointsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Create Checkpoint Panel -->
        <Border Grid.Row="0" Background="{StaticResource CardBrush}" Padding="15" Margin="10" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Lab:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                <TextBox Grid.Column="1" Text="{Binding LabName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,15,0"/>
                <TextBlock Grid.Column="2" Text="Checkpoint Name:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                <TextBox Grid.Column="3" Text="{Binding CheckpointName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,15,0"/>
                <Button Grid.Column="4" Content="📸 Create Checkpoint" Command="{Binding CreateCheckpointCommand}" Padding="15,6"/>
            </Grid>
        </Border>

        <!-- Action Buttons -->
        <Border Grid.Row="1" Background="{StaticResource CardBrush}" Padding="10" Margin="10,0,10,10" CornerRadius="5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="🔄 Refresh" Command="{Binding RefreshCommand}" Width="100" Height="32" Margin="5" Style="{StaticResource UniformButton}"/>
                <Button Content="↩️ Rollback" Command="{Binding RollbackCommand}" Width="100" Height="32" Margin="5" Style="{StaticResource UniformButton}"/>
                <Button Content="🔍 Compare" Command="{Binding CompareCommand}" Width="100" Height="32" Margin="5" Style="{StaticResource UniformButton}"/>
                <Button Content="🗑 Delete" Command="{Binding DeleteCommand}" Width="100" Height="32" Margin="5" Style="{StaticResource RedButton}"/>
            </StackPanel>
        </Border>

        <!-- Checkpoints List -->
        <Border Grid.Row="2" Background="{StaticResource CardBrush}" Margin="10,0,10,10" CornerRadius="5" Padding="10">
            <ListView ItemsSource="{Binding Checkpoints}" SelectedItem="{Binding SelectedCheckpoint}" SelectionMode="Single" BorderThickness="0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Status" Width="120" DisplayMemberBinding="{Binding DisplayStatus}"/>
                        <GridViewColumn Header="Name" Width="200" DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="Description" Width="200" DisplayMemberBinding="{Binding Description}"/>
                        <GridViewColumn Header="Created" Width="120" DisplayMemberBinding="{Binding TimeSinceCreation}"/>
                        <GridViewColumn Header="VMs" Width="60" DisplayMemberBinding="{Binding Snapshots.Count}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</UserControl>