<Window x:Class="OpenCodeLab.Views.TemplateGalleryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lab Template Gallery" Height="550" Width="750" WindowStartupLocation="CenterOwner" ResizeMode="CanResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Choose a Lab Template" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Template List -->
        <ListView Grid.Row="1" Name="TemplateList" SelectionMode="Single" SelectionChanged="TemplateList_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="" Width="40" DisplayMemberBinding="{Binding IconGlyph}"/>
                    <GridViewColumn Header="Name" Width="180" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Description" Width="250" DisplayMemberBinding="{Binding Description}"/>
                    <GridViewColumn Header="Category" Width="100" DisplayMemberBinding="{Binding Category}"/>
                    <GridViewColumn Header="VMs" Width="45" DisplayMemberBinding="{Binding Config.VMs.Count}"/>
                    <GridViewColumn Header="Type" Width="70">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="User"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsBuiltIn}" Value="True">
                                                    <Setter Property="Text" Value="Built-in"/>
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Name="DeleteButton" Content="Delete" Click="DeleteButton_Click" Width="80" Height="32" Margin="5" IsEnabled="False"/>
            <Button Name="CancelButton" Content="Cancel" Click="CancelButton_Click" Width="80" Height="32" Margin="5"/>
            <Button Name="UseButton" Content="Use Template" Click="UseButton_Click" Width="120" Height="32" Margin="5" IsEnabled="False"
                    Background="SteelBlue" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>