<UserControl x:Class="OpenCodeLab.Views.HealthDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource CardBrush}" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Lab Name:" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBox x:Name="LabNameTextBox" Width="200" Text="{Binding LabName, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Padding="5"/>
                <Button Content="Check Health" Command="{Binding RunHealthCheckCommand}" Width="100" Margin="10,0,5,0" Padding="5" Style="{StaticResource UniformButton}"/>
                <Button Content="Export" Command="{Binding ExportReportCommand}" Width="80" Margin="5,0" Padding="5" Style="{StaticResource UniformButton}"/>
                <CheckBox Content="Auto-refresh" IsChecked="{Binding AutoRefresh}" VerticalAlignment="Center" Margin="15,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="10">
                <!-- Overall Status Card -->
                <Border Background="{StaticResource CardBrush}" Padding="15" CornerRadius="5">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="Overall Status" FontWeight="SemiBold" FontSize="14" Margin="0,0,15,0"/>
                            <TextBlock Text="{Binding OverallStatusEmoji}" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding OverallStatus}" FontSize="14" Foreground="{Binding OverallStatusColor}" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Stats Row -->
                        <Grid Margin="0,10,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="✅" FontSize="16" Margin="0,0,5,0" Foreground="Green"/>
                                <TextBlock Text="{Binding HealthyCount}" FontWeight="Bold" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="⚠️" FontSize="16" Margin="0,0,5,0" Foreground="Orange"/>
                                <TextBlock Text="{Binding WarningCount}" FontWeight="Bold" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="🔴" FontSize="16" Margin="0,0,5,0" Foreground="Red"/>
                                <TextBlock Text="{Binding CriticalCount}" FontWeight="Bold" FontSize="18"/>
                            </StackPanel>
                        </Grid>

                        <!-- Last Checked -->
                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                            <TextBlock Text="Last checked: " FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock Text="{Binding LastChecked}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Alerts Section -->
                <Border Background="{StaticResource CardBrush}" Padding="15" Margin="0,10,0,0" CornerRadius="5">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="Active Alerts" FontWeight="SemiBold" FontSize="14"/>
                            <TextBlock Text="{Binding AlertCount}" Foreground="{StaticResource PrimaryBrush}" Margin="10,0,0,0" FontWeight="Bold"/>
                        </StackPanel>

                        <ItemsControl ItemsSource="{Binding ActiveAlerts}" MaxHeight="200">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="5">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding Title}" FontWeight="Medium" TextWrapping="Wrap"/>
                                                <TextBlock Text="{Binding Message}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" FontSize="11"/>
                                            </StackPanel>
                                            <Button Grid.Column="1" Content="Dismiss" Command="{Binding DataContext.AcknowledgeAlertCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Padding="5" Background="Transparent" BorderThickness="0" FontSize="11" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Button Content="Dismiss All" Command="{Binding AcknowledgeAllAlertsCommand}" Padding="8,4" Margin="0,10,0,0" HorizontalAlignment="Right" Style="{StaticResource UniformButton}"/>
                    </StackPanel>
                </Border>

                <!-- VM Health Grid -->
                <Border Background="{StaticResource CardBrush}" Padding="15" Margin="0,10,0,0" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="VM Health Status" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>
                        <ListView ItemsSource="{Binding VmHealthStatuses}" BorderThickness="0" MaxHeight="300">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="VM Name" Width="150" DisplayMemberBinding="{Binding VmName}"/>
                                    <GridViewColumn Header="State" Width="100" DisplayMemberBinding="{Binding State}"/>
                                    <GridViewColumn Header="Health" Width="80">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding HealthEmoji}" FontSize="14"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Issues" Width="80" DisplayMemberBinding="{Binding Issues.Count}"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </StackPanel>
                </Border>

                <!-- Status Bar -->
                <Border Background="{StaticResource CardBrush}" Padding="10" Margin="0,10,0,0" CornerRadius="5">
                    <TextBlock Text="{Binding StatusMessage}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
