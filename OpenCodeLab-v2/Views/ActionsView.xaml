<UserControl x:Class="OpenCodeLab.Views.ActionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <!-- Lab Actions -->
        <Border Grid.Row="0" Background="{StaticResource CardBrush}" Padding="15" Margin="10" CornerRadius="5">
            <StackPanel>
                <TextBlock Text="Lab Management" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="New Lab" Command="{Binding NewLabCommand}" Height="35" Margin="2"/>
                    <Button Grid.Column="1" Content="Edit Lab" Command="{Binding EditLabCommand}" Height="35" Margin="2"/>
                    <Button Grid.Column="2" Content="Deploy" Command="{Binding DeployLabCommand}" Height="35" Margin="2"/>
                    <Button Grid.Column="3" Content="Remove" Command="{Binding RemoveLabCommand}" Height="35" Margin="2"/>
                </Grid>
                <Grid Margin="0,5,0,0">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDeploying}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <TextBlock Text="Logs saved to: &lt;Configured LabSources&gt;\Logs\deployment-YYYYMMDD-HHMMSS.log" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"/>
                </Grid>
                <Grid Margin="0,5,0,0">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDeploying}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Cancel Deployment" Command="{Binding CancelDeployCommand}" Height="30" Background="#8B0000" Foreground="White" FontSize="11" Padding="15,2"/>
                        <TextBlock Text="Cancelling..." Margin="10,0,0,0" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"
                                   Visibility="{Binding IsCancellationRequested, Converter={StaticResource BoolToVis}}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Lab List -->
        <Border Grid.Row="1" Background="{StaticResource CardBrush}" Padding="15,10" Margin="10,0,10,10" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Recent Labs" FontSize="14" FontWeight="Bold"/>
                <TextBlock Grid.Column="1" Text="{Binding SelectedLab.LabName, StringFormat='Selected: {0}'}" FontSize="12" Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="{StaticResource CardBrush}" Margin="10,0,10,10" CornerRadius="5" Padding="5">
            <ListBox ItemsSource="{Binding RecentLabs}" SelectedItem="{Binding SelectedLab}" Padding="5" BorderThickness="0" Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5">
                            <TextBlock Text="{Binding LabName}" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="{Binding Description}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                            <TextBlock FontSize="10" Foreground="Gray">
                                <Run Text="VMs:"/>
                                <Run Text="{Binding VMs.Count, Mode=OneWay}"/>
                                <Run Text="| Domain:"/>
                                <Run Text="{Binding DomainName, Mode=OneWay}"/>
                                <Run Text="| Switch:"/>
                                <Run Text="{Binding Network.SwitchType, Mode=OneWay}"/>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Deployment Progress & Logs -->
        <Border Grid.Row="3" Background="{StaticResource CardBrush}" Margin="10" CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="10,10,10,5">
                    <TextBlock Text="Deployment Log" FontSize="14" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Copy" Command="{Binding CopyLogsCommand}" Padding="10,4" FontSize="11" Margin="0,0,5,0"/>
                        <Button Content="Clear" Command="{Binding ClearLogsCommand}" Padding="10,4" FontSize="11"/>
                    </StackPanel>
                </Grid>
                <ProgressBar Grid.Row="1" Height="20" Minimum="0" Maximum="100" Value="{Binding DeploymentProgress}" Margin="10,0,10,10"/>
                <TextBlock Grid.Row="2" Text="{Binding DeploymentProgress, StringFormat='Progress: {0}%'}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Right" Margin="0,0,10,5"/>
                <Grid Grid.Row="3" Margin="10,0,10,10">
                    <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="3" Background="#FAFAFA">
                        <TextBox x:Name="LogTextBox" Text="{Binding LogOutput}" FontFamily="Consolas" FontSize="11" Padding="8"
                                 IsReadOnly="True" TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                                 VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                                 BorderThickness="0" Background="Transparent"
                                 TextChanged="LogTextBox_TextChanged"/>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
