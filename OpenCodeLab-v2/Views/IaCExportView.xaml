<UserControl x:Class="OpenCodeLab.Views.IaCExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource CardBrush}" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Lab:" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding LabName, UpdateSourceTrigger=PropertyChanged}" Width="150" Padding="5" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0">
                    <TextBlock Text="Format:" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <ComboBox ItemsSource="{Binding FormatOptions}" SelectedItem="{Binding SelectedFormat}" Width="120" Padding="5"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="20,0,0,0">
                    <TextBlock Text="Output:" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding OutputPath, UpdateSourceTrigger=PropertyChanged}" Width="200" Padding="5"/>
                </StackPanel>
                <Button Grid.Column="4" Content="Generate Preview" Command="{Binding PreviewCommand}" Width="120" Padding="5" Style="{StaticResource UniformButton}"/>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Preview -->
            <Border Grid.Row="0" Background="{StaticResource CardBrush}" Padding="15" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Preview" FontWeight="SemiBold" FontSize="14"/>
                        <TextBlock Text="{Binding SelectedFormat, StringFormat=' - {0}'}" Foreground="{StaticResource TextSecondaryBrush}" Margin="5,0,0,0"/>
                        <TextBlock Text="(No preview generated)" Foreground="{StaticResource TextSecondaryBrush}" Margin="10,0,0,0" Visibility="{Binding HasPreview, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}"/>
                    </StackPanel>
                    <TextBox Grid.Row="1" Text="{Binding PreviewContent}" IsReadOnly="True" TextWrapping="Wrap" 
                             AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                             FontFamily="Consolas" FontSize="12" Background="#FAFAFA" Padding="10"/>
                </Grid>
            </Border>

            <!-- Action Buttons -->
            <Border Grid.Row="1" Background="{StaticResource CardBrush}" Padding="15" Margin="0,10,0,0" CornerRadius="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Copy to Clipboard" Command="{Binding CopyCommand}" Width="120" Margin="0,0,10,0" Padding="5" Style="{StaticResource UniformButton}"/>
                        <Button Content="Export to File" Command="{Binding ExportCommand}" Width="110" Padding="5" Style="{StaticResource UniformButton}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
