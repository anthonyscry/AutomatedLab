<UserControl x:Class="OpenCodeLab.Views.BaselineManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource CardBrush}" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Lab Name:" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBox x:Name="LabNameTextBox" Width="200" Text="{Binding LabName, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Padding="5"/>
                <Button Content="Capture Baseline" Command="{Binding CaptureBaselineCommand}" Width="120" Margin="10,0,5,0" Padding="5" Style="{StaticResource UniformButton}"/>
                <Button Content="Check Drift" Command="{Binding CheckDriftCommand}" Width="100" Margin="5,0" Padding="5" Style="{StaticResource UniformButton}"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Baselines List -->
            <Border Grid.Column="0" Background="{StaticResource CardBrush}" Padding="10" Margin="0,0,10,0" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Baselines" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>

                    <ListView Grid.Row="1" ItemsSource="{Binding Baselines}" SelectedItem="{Binding SelectedBaseline}" BorderThickness="0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="8">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <CheckBox IsChecked="{Binding IsGoldenBaseline}" Margin="0,0,5,0" IsEnabled="False"/>
                                            <TextBlock Text="{Binding Name}" FontWeight="Medium"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" Margin="20,2,0,0"/>
                                        <TextBlock Text="{Binding VmCount, StringFormat='{}{0} VMs'}" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" Margin="20,2,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>

            <!-- Selected Baseline Details -->
            <Border Grid.Column="1" Background="{StaticResource CardBrush}" Padding="15" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Baseline Details" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>

                    <StackPanel Grid.Row="1" Visibility="{Binding HasSelectedBaseline, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="{Binding SelectedBaselineInfo}" TextWrapping="Wrap" LineHeight="22"/>

                        <!-- Drift Status -->
                        <Border Background="#F5F5F5" Padding="10" CornerRadius="5" Margin="0,15,0,0">
                            <StackPanel>
                                <TextBlock Text="Drift Status" FontWeight="Medium" Margin="0,0,0,5"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding DriftStatusText}" FontWeight="Bold" Foreground="{Binding DriftStatusColor}"/>
                                    <TextBlock Text="{Binding DriftCount, StringFormat=' - {0} items'}" Margin="10,0,0,0" Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- New Baseline Description -->
                        <TextBlock Text="Description for new baseline:" Margin="0,15,0,5" FontWeight="Medium"/>
                        <TextBox Text="{Binding NewBaselineDescription, UpdateSourceTrigger=PropertyChanged}" Padding="5" AcceptsReturn="True" Height="60" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                    </StackPanel>


                    <!-- Action Buttons -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,15,0,0" HorizontalAlignment="Right">
                        <Button Content="Delete" Command="{Binding DeleteBaselineCommand}" Padding="10,5" Margin="0,0,10,0" Style="{StaticResource RedButton}"/>
                        <Button Content="Export" Command="{Binding ExportBaselineCommand}" Padding="10,5" Margin="0,0,10,0" Style="{StaticResource UniformButton}"/>
                        <Button Content="Set as Golden" Command="{Binding SetGoldenCommand}" Padding="10,5" Style="{StaticResource UniformButton}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="1" Background="{StaticResource CardBrush}" Padding="10" VerticalAlignment="Bottom" Margin="10" CornerRadius="5">
            <TextBlock Text="{Binding StatusMessage}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
        </Border>
    </Grid>
</UserControl>
