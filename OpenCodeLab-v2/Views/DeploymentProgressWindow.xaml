<Window x:Class="OpenCodeLab.Views.DeploymentProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Deploying Lab" Height="300" Width="500" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Deployment Progress" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
        <ProgressBar Grid.Row="1" Name="ProgressBar" Height="25" Minimum="0" Maximum="100" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="2" Name="StatusText" Text="Initializing..." VerticalAlignment="Top" Padding="5"/>
        <Button Grid.Row="3" Content="Close" Name="CloseButton" Click="CloseButton_Click" Width="80" Height="30" HorizontalAlignment="Right" Margin="0,10,0,0"/>
    </Grid>
</Window>
