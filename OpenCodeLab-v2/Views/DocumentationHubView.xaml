<UserControl x:Class="OpenCodeLab.Views.DocumentationHubView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource CardBrush}" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="Search:" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Width="300" Padding="5" VerticalAlignment="Center"/>
                        <Button Content="Search" Command="{Binding SearchCommand}" Width="80" Margin="10,0,5,0" Padding="5" Style="{StaticResource UniformButton}"/>
                        <Button Content="Reindex" Command="{Binding RefreshIndexCommand}" Width="80" Margin="5,0" Padding="5" Style="{StaticResource UniformButton}"/>
                    </StackPanel>
                    <Button Grid.Column="1" Content="Export" Command="{Binding ExportCommand}" Width="80" Padding="5" Style="{StaticResource UniformButton}"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Search Results & Decisions -->
            <Grid Grid.Column="0" Margin="0,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Search Results -->
                <Border Grid.Row="0" Background="{StaticResource CardBrush}" Padding="10" CornerRadius="5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Documentation" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>
                        <ListBox Grid.Row="1" ItemsSource="{Binding SearchResults}" SelectedItem="{Binding SelectedSearchResult}" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="8">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding CategoryEmoji}" FontSize="14" Margin="0,0,5,0"/>
                                                <TextBlock Text="{Binding Title}" FontWeight="Medium" TextTrimming="CharacterEllipsis" MaxWidth="200"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Description}" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" TextTrimming="CharacterEllipsis" MaxWidth="250" Margin="0,2,0,0"/>
                                            <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                                <TextBlock Text="{Binding SourceType}" FontSize="9" Foreground="{StaticResource TextSecondaryBrush}"/>
                                                <TextBlock Text=" • " FontSize="9" Foreground="{StaticResource TextSecondaryBrush}"/>
                                                <TextBlock Text="{Binding UpdatedAt, StringFormat='{}{0:MM/dd/yyyy}'}" FontSize="9" Foreground="{StaticResource TextSecondaryBrush}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <!-- Decision Records -->
                <Border Grid.Row="1" Background="{StaticResource CardBrush}" Padding="10" Margin="0,10,0,0" CornerRadius="5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Decision Log (ADRs)" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>
                        <ListBox Grid.Row="1" ItemsSource="{Binding DecisionRecords}" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="8">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding StatusEmoji}" FontSize="14" Margin="0,0,5,0"/>
                                                <TextBlock Text="{Binding ShortId}" FontWeight="Bold" FontSize="11"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Title}" FontSize="11" TextTrimming="CharacterEllipsis" MaxWidth="250"/>
                                            <TextBlock Text="{Binding Status}" FontSize="9" Foreground="{StaticResource TextSecondaryBrush}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>

            <!-- Right Panel - Document Viewer & Tools -->
            <Border Grid.Column="1" Background="{StaticResource CardBrush}" Padding="15" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Document Header -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Document Viewer" FontWeight="SemiBold" FontSize="14"/>
                        <TextBlock Text="{Binding SelectedDocument.Title, StringFormat=' - {0}'}" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="5,0,0,0"/>
                    </StackPanel>

                    <!-- Document Content -->
                    <TextBox Grid.Row="1" Text="{Binding DocumentContent}" IsReadOnly="True" TextWrapping="Wrap" 
                             AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                             FontFamily="Consolas" FontSize="12" Background="#FAFAFA" Padding="10"/>

                    <!-- Tools Section -->
                    <Grid Grid.Row="2" Margin="0,15,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Create Decision Record -->
                        <Border Grid.Row="0" Background="#F5F5F5" Padding="10" CornerRadius="5">
                            <StackPanel>
                                <TextBlock Text="Create Decision Record" FontWeight="Medium" Margin="0,0,0,10"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Title:" VerticalAlignment="Center" Margin="0,0,5,5"/>
                                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NewDecisionTitle, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,0,5"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Context:" VerticalAlignment="Top" Margin="0,0,5,5"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NewDecisionContext, UpdateSourceTrigger=PropertyChanged}" Padding="5" Height="50" AcceptsReturn="True" TextWrapping="Wrap" Margin="0,0,0,5"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Decision:" VerticalAlignment="Top" Margin="0,0,5,5"/>
                                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding NewDecisionDecision, UpdateSourceTrigger=PropertyChanged}" Padding="5" Height="50" AcceptsReturn="True" TextWrapping="Wrap" Margin="0,0,0,5"/>

                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Consequences:" VerticalAlignment="Top" Margin="0,0,5,5"/>
                                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding NewDecisionConsequences, UpdateSourceTrigger=PropertyChanged}" Padding="5" Height="50" AcceptsReturn="True" TextWrapping="Wrap" Margin="0,0,0,5"/>
                                </Grid>
                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <TextBox Text="{Binding NewDecisionLabName, UpdateSourceTrigger=PropertyChanged}" Width="150" Padding="5" Margin="0,0,10,0" ToolTip="Optional: Related lab name"/>
                                    <Button Content="Create ADR" Command="{Binding CreateDecisionRecordCommand}" Padding="10,5" Style="{StaticResource UniformButton}"/>
                                    <Button Content="Generate Onboarding" Command="{Binding GenerateOnboardingGuideCommand}" Padding="10,5" Margin="10,0,0,0" Style="{StaticResource UniformButton}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="1" Background="{StaticResource CardBrush}" Padding="10" VerticalAlignment="Bottom" Margin="10" CornerRadius="5">
            <TextBlock Text="{Binding StatusMessage}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
        </Border>
    </Grid>
</UserControl>
