---
description: Build and run OpenCodeLab app
disable-model-invocation: true
---

Run this command from the repository root:

`pwsh -NoProfile -ExecutionPolicy Bypass -File "./Scripts/Run-OpenCodeLab.ps1" $ARGUMENTS`

If no arguments are provided, the launcher defaults to interactive menu mode.
